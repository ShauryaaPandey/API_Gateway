FRONT-END -- MIDDLEEND -- BACKEND

--we need an intermediate layer between the client and the microservice

--using this middleware end , when client sends request we will be able to make decision that which mircoervice actually responds to this request

--we can do message validation , response tranformation , rate limiting 

--we try to prepare and API gateway that acts as this middle end 